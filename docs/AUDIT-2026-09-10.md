# 完整审计报告：Test Observatory 当前真实状态

日期：2026-09-10
审计方式：读源码 + 跑覆盖率 + 跑测试 + 核实每一条声明
审计范围：`packages/test-runner/test-observatory`（21 个源文件）

## 1. 代码与测试规模（实测）

```
源码  2862 行 / 21 文件
测试  1273 行 / 9 文件
测试/源码比 = 0.44
```

## 2. 覆盖率实测（重要发现）

命令：`pnpm exec vitest run --coverage --coverage.include='packages/test-runner/test-observatory/src/**' packages/test-runner/test-observatory`

```
总体：语句 79.45%  分支 72.78%  函数 79.65%  行 81.46%
```

仓库 CI 门禁（`vitest.config.ts:348-355`）要求 **每个文件 100%**（`perFile: true`，四项均 100）。

### 2.1 关键未覆盖清单

| 文件 | 语句 | 分支 | 函数 | 性质 |
|---|---:|---:|---:|---|
| `src/command/index.ts` | **0** | **0** | **0** | **`/test` 命令真实装配与执行流程** |
| `src/tool/index.ts` | **0** | **0** | **0** | **`run_tests` 工具真实装配与执行流程** |
| `src/experience/a11y.ts` | 69.23 | 100 | 60 | axe 接入 |
| `src/command/structured.ts` | 84.31 | 72.07 | 89.65 | 结构化解析 |
| `src/command/experience.ts` | 70.37 | 62.5 | 91.66 | 体验映射 |
| `src/report/render.ts` | 82.6 | 85.71 | 100 | 报告渲染 |
| `src/experience/runner.ts` | 95.45 | 84.05 | 93.75 | 浏览器旅程执行 |
| `src/tool/report.ts` | 91.66 | 100 | 80 | 工具报告适配 |
| `src/experience/scoring.ts` | 98.07 | 88.88 | 100 | 评分 |
| `src/command/history.ts` | 91.89 | 100 | 90 | 历史 |
| `src/command/config.ts` | 95.58 | 91.26 | 100 | 配置校验 |

（`src/index.ts`、`src/report/index.ts`、`src/experience/index.ts`、各 `types.ts` 为纯再导出或纯类型，显示 0% 属正常，不计入缺口。）

### 2.2 结论

**两个最关键的真实入口——`/test` 命令与 `run_tests` 工具——覆盖率是 0%。**
这意味着：
- 我此前多轮使用「/test 是真实运行验证过的」这一说法，那是**通过手工脚本**跑通的，不是通过测试保障的；
- 任何改动都可能静默破坏这两个入口而不被测试发现；
- 这也解释了为什么上一轮那个 verdict 假通过 bug 能在 106 项测试全绿的情况下存在。

## 3. 已核实的功能缺口

### 3.1 数据模型层面（导致「画出重要部分」不可能）

| 事实 | 位置 |
|---|---|
| `VisualViolation` 只有 rule/detail/severity，**无元素、无坐标** | `src/experience/visual.ts:14-21` |
| 检测时明明持有元素对象，却只把 `src` 写进字符串，元素被丢弃 | `src/experience/visual.ts:33-40` |
| axe 每个 violation 自带 `nodes[].target`，代码只取 `nodes.length` 这个数字 | `src/experience/a11y.ts:36-41` |
| `CapturedShot` 只有 `dataUri` 与文案，**无任何几何字段** | `src/experience/types.ts:43-60` |

### 3.2 截图正确性层面

| 事实 | 位置 |
|---|---|
| `captureBounded` 只做尺寸降级，**无任何忠实性校验** | `src/experience/runner.ts:35-52` |
| 无坐标一致性校验、无动态区域屏蔽、无平台差异处理 | 全项目 |

### 3.3 真人模拟层面（全部未实现）

| 事实 | 证据 |
|---|---|
| `JourneySpec.persona` 是 `string`，仅显示名 | `src/experience/types.ts:29-40` |
| 所有 journey 共用同一套 `runJourney`/`runAction`，无任何 persona 分支 | `src/experience/runner.ts:170-221` |
| 动作原语只有 7 种，全部为鼠标/断言类，**无键盘、无 reload、无节流** | `src/experience/types.ts:9-16` |
| 无 persona 策略层、无边界输入、无环境模拟、无恢复路径 | 全项目 |

### 3.4 已修复的缺口（不重复计）

| 项 | 状态 |
|---|---|
| verdict 假通过（测试失败但显示已通过） | 已修 + 回归测试 |
| 附件渲染 XSS | 已修 + 回归测试 |
| pytest 类路径推断错误 | 已修 + 测试 |
| 通过用例退出码证据丢失 | 已修 + 测试 |
| 长耗时格式不一致 | 已修 + 复用 |
| 历史投影后 causes/slowest 不重算 | **已修**（`src/command/index.ts:109-110`，本次审计更正了我此前的说法） |
| 空 evidence/findings 区块 | 已修 |

### 3.5 仍然存在的缺口

| # | 缺口 | 影响 | 优先级 |
|---|---|---|---|
| G1 | `/test` 与 `run_tests` 入口覆盖率 0% | 改动可能静默破坏主入口 | **高** |
| G2 | 每文件 100% 覆盖率门禁未满足 | 无法通过仓库 CI | **高** |
| G3 | 历史文件无配置身份 | 测试集合变化时趋势误导读者 | 中 |
| G4 | monorepo 集成改动未提交 | 包注册/类型配置/Agent Note 仍在工作区 | 中 |
| G5 | finding 无 new/known/muted 分类 | 每次运行把老问题当新发现 | 中低 |
| G6 | `/test auto` 检测能力有限 | 只覆盖部分框架 | 低 |
| G7 | `causes` 只有单一硬编码标签 | 失败聚类粗糙 | 低 |
| G8 | 视觉基线比对未实现 | 无法判断界面是否正确 | 中（已设计，默认不纳入） |

## 4. 已完成的发布动作（核实）

| 项 | 状态 |
|---|---|
| standalone 仓库构建 | ✅ Build complete，11 文件 141.09 kB |
| 隔离 DSH_HOME tarball 安装 | ✅ 安装成功，cordis 组合正确 |
| 隔离环境真实 `/test` | ✅ 1/1 passed，报告 53688 字节且 CSS 完整 |
| GitHub 推送 | ✅ 最新 `e650cac` |
| npm 发布 | ❌ 403，需 OTP |
| dsh-market topic | ❌ 仓库无 topic，需网页操作 |
| 仓库满 24h | ❌ 创建于 2026-09-09T23:18:44Z |

## 5. 测试现状的诚实表述

| 说法 | 准确性 |
|---|---|
| `108 项测试全通过` | ✅ 准确（106 default + 2 需设置 `DSH_BROWSER_EXECUTABLE` 的真实浏览器测试） |
| `测试全绿` | ⚠️ 需限定：只覆盖已实现部分；两个真实入口为 0% |
| `/test 已真实验证` | ⚠️ 需限定：通过手工脚本验证，非测试保障 |
| `覆盖率达标` | ❌ **不成立**，79% vs 门禁 100% |

## 6. 审计结论

### 6.1 工作流 A 完成后的更新（2026-09-10 晚）

工作流 A（工程风险修复）已执行，实测变化：

| 指标 | 修复前 | 修复后 |
|---|---:|---:|
| 测试数 | 108 | **218**（216 通过 + 2 需浏览器） |
| 总体语句覆盖 | 79.45% | **95.5%** |
| 总体分支覆盖 | 72.78% | **90.4%** |
| 总体函数覆盖 | 79.65% | **95.5%** |
| `/test` 入口语句覆盖 | **0%** | **96.9%** |
| `run_tests` 入口覆盖 | **0%** | **100%** |

新增测试文件：`command-entry`、`tool-entry`、`structured-branches`、`experience-mapping`、`entry-branches`、`remaining-branches`、`runner-and-a11y`、`last-branches`。

完全达标的文件（6/15）：`config.ts`、`detect.ts`、`command/experience.ts`、`report/assets.generated.ts`、`tool/index.ts`、`tool/report.ts`。

### 6.2 修复过程中发现并修正的真实缺陷

1. **配置校验与自身文档不符**：`optionalPositiveInt` 的名称与 JSDoc 都声明「正数」，实现却允许 0，错误文案写成「non-negative」。零值超时/等待/视口尺寸都是配置错误。已拆分为 `optionalNonNegativeInt`（仅用于 `expectedExitCode`，退出码 0 是正常成功）与 `optionalPositiveInt`（其余全部要求 > 0）。

### 6.3 未能达到每文件 100% 的诚实说明

剩余 9 个文件未达门禁，缺口分为三类：

| 类型 | 例子 | 是否可通过测试消除 |
|---|---|---|
| **V8 插桩限制** | `a11y.ts` 中传给 `page.evaluate` 的序列化函数体；测试用 `new Function` 重建后 V8 无法把执行映射回原行 | 否（除非改动源码结构） |
| **依赖运行期真实浏览器** | `runner.ts` 的截图降级循环、`visual.ts` 依赖真实布局计算的判空分支 | 需真实浏览器测试 |
| **需劫持不可变模块导出** | `render.ts` 的 HTML 标记缺失回退路径（需要替换 `REPORT_BODY` 常量） | 需重构才能测 |

**这部分我没有解决，也不声称已解决。** 但它不改变结论：两个真实入口从零覆盖提升到 96.9% / 100%，是本次最重要的工程质量改善。

**当前实现的工程质量是「核心逻辑测试较扎实、两个真实入口已补测试但覆盖率未达仓库每文件 100% 门禁」。**

具体地：
- 解析/历史/评分/视觉检查这些**纯逻辑模块**测试较好（84%–100%）；
- 但**用户与模型真正调用的两个入口**（`/test`、`run_tests`）零覆盖，这是最大的工程风险；
- 真人模拟**一行未实现**；
- 截图标注与忠实性校验**一行未实现**，且数据模型缺失是根因；
- 发布被两个外部条件阻塞。